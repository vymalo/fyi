# UI Package

Shared UI primitives consumed by frontend apps.

- Tailwind-first React components that play nicely with shadcn/ui utilities and daisyUI themes.
- Exports `Button`, `Card`, and a `cn` helper for merging classes.
- Published locally as `@vymalo/ui` via the workspace.
