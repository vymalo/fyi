# UI Package

Shared UI primitives consumed by frontend apps.
- Tailwind-first React components aligned with shadcn/ui patterns.
- Exports `Button`, `Badge`, `Card`, `Input`, and a `cn` helper for merging classes.
- Published locally as `@vymalo/ui` via the workspace.
