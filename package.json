{
  "name": "vymalo-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@8.15.8",
  "scripts": {
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.4"
  }
}
